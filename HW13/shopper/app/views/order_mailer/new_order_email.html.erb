<h1>Order confirmation</h1>
<%= @user.email%>, thank you for your order. We've recevied your order and will contact you as your package is shipped. You can find your purchase information below.
<p>
Order details<br>
--------------------------
</p>
<table>
  <thead>
    <tr>
      <th></th>
      <th>Image</th>
      <th>Name</th>
      <th>Quantity</th>
      <th>Price</th>
    </tr>
  </thead>
  <tbody>
  <% @line_items.each.with_index do |line_item, index| %>
    <tr>
      <td><%= index + 1 %></td>
      <th>
       <% if line_item.product.image.attached? %>
          <%= image_tag line_item.product.image, height: 80, height: 80 %>
          <% else %>
          <%= image_tag("no_image_item.jpeg", height: 80, height: 80) %>
        <% end %>
      </th>
      <td><%= line_item.product.name %></td>
      <td>
        <%= line_item.quantity %>
      </td>
      <td><%= number_to_currency(line_item.total_price) %></td>
    </tr>
    <% end %>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td>Total price: <%= number_to_currency @order.cart.total_price %></td>
  </tbody>
</table>
